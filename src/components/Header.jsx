import React from "react";

const Header = () => {
  return (
    <section className="hero">
      <div className="container hero-content">
        <p className="hero-tags">GET SCHOOLED</p>
        <h1 className="hero-title">Smile School</h1>
        <button className="hero-button" type="button">
          REGISTER FOR FREE
        </button>
        <p className="hero-subtitle">Learn from the pros</p>
      </div>
    </section>
  );
};

export default Header;
